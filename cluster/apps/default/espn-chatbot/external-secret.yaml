apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: espn-chatbot
  namespace: default
spec:
  target:
    template:
      metadata:
        labels: {}
  dataFrom:
    - extract:
        key: espn-chatbot

  refreshInterval: 24h
  secretStoreRef:
    kind: ClusterSecretStore
    name: secretstore
